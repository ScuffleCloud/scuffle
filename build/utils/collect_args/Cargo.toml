[package]
name = "collect_args"
version = "0.1.0"
edition = "2024"
license = "MIT OR Apache-2.0"

[dependencies]
serde_json = "1"
camino = "1"
clap = { version = "4.5", features = ["derive", "env"] }
