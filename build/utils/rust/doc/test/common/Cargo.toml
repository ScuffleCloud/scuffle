[package]
name = "rust_doctest_common"
publish = false
edition = "2024"
license = "MIT OR Apache-2.0"

[dependencies]
camino = { version = "1", features = ["serde1"] }
serde = "1"
serde_derive = "1"
serde_json = "1"

[package.metadata.sync-readme.badges]
license = true
