[package]
name = "sync_readme_test_runner"
edition = "2024"
publish = false
license = "MIT OR Apache-2.0"

[dependencies]
camino = "1"
serde_json = "1"
similar = { version = "2.7", features = ["inline"] }
console = "0.16"
anyhow = "1"
clap = { version = "4.5", features = ["derive", "env"] }
sync_readme_common.path = "../common"

[package.metadata.sync-readme.badges]
license = true
