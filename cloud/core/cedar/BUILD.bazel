load("//misc/utils/rust:manifest.bzl", "cargo_toml")
load("//misc/utils/rust:package.bzl", "scuffle_build_script", "scuffle_package")

cargo_toml()

scuffle_build_script(
    name = "build_script",
    data = ["//cloud/core:static_policies.cedarschema"],
    deps = [
        "//crates/cedar-policy/codegen",
    ],
)

scuffle_package(
    crate_name = "scufflecloud-core-cedar",
    deps = [
        ":build_script",
        "//crates/cedar-policy",
    ],
)
