load("//misc/utils/rust:diesel_migration.bzl", "diesel_migration")
load("//misc/utils/rust:manifest.bzl", "cargo_toml")
load("//misc/utils/rust:package.bzl", "scuffle_package")

cargo_toml()

scuffle_package(
    aliases = {
        "//cloud/id": "id",
    },
    crate_name = "scufflecloud-core-db-types",
    deps = [
        "//cloud/id",
    ],
)

diesel_migration(
    name = "migration",
    config_file = "diesel.toml",
    data = glob([
        "migrations/**/*.sql",
    ]),
    database_image = "@postgres18",
    schema_file = "src/schema.rs",
    schema_patch_file = "src/schema.patch",
)
