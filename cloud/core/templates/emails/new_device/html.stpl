We noticed a new login to your Scuffle account.
IP: <%= self.ip_address %>
<% if let Some(geo_info) = self.geo_info.as_ref() { %>
From <% if let Some(city) = geo_info.city.as_ref() { %><%= city %>, <% } %><% if let Some(country) = geo_info.country.as_ref() { %><%= country %><% } %>
<% } %>
