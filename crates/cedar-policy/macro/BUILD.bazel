load("//misc/utils/rust:manifest.bzl", "cargo_toml")
load("//misc/utils/rust:package.bzl", "scuffle_package")

cargo_toml()

scuffle_package(
    crate_name = "scuffle-cedar-policy-macro",
    crate_type = "proc_macro",
    deps = [
        "//crates/cedar-policy/codegen"
    ],
)
