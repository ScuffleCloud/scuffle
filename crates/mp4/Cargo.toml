[package]
name = "scuffle-mp4"
version = "0.1.5"
authors = ["Scuffle <opensource@scuffle.cloud>"]
documentation = "https://docs.rs/scuffle-mp4"
edition = "2024"
keywords = ["mp4", "video", "codec", "isobmff"]
license = "MIT OR Apache-2.0"
readme = "README.md"
repository = "https://github.com/scufflecloud/scuffle"
description = "A pure Rust implementation of MP4 file format ISOBMFF boxes"

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ['cfg(coverage_nightly)'] }

[features]
## Enables changelog and documentation of feature flags
docs = ["dep:scuffle-changelog", "dep:document-features"]

[dependencies]
document-features = { optional = true, version = "0.2" }
isobmff = { path = "../isobmff", version = "0.1" }
isobmff-derive = { path = "../isobmff/derive", version = "0.1" }
nutype-enum = { path = "../nutype-enum", version = "0.1" }
scuffle-bytes-util = { path = "../bytes-util", version = "0.1" }
scuffle-changelog = { optional = true, path = "../changelog", version = "0.1" }

[dev-dependencies]
serde = { features = ["derive"], version = "1" }
serde_json = "1"

[package.metadata.docs.rs]
all-features = true
rustdoc-args = [
    "--cfg",
    "docsrs",
    "--sort-modules-by-appearance",
    "--generate-link-to-definition",
]

[package.metadata.xtask.powerset]
additive-features = ["docs"]

[package.metadata.sync-readme.rustdoc-mappings]
changelog = "./CHANGELOG.md"

[package.metadata.sync-readme.badges]
docs-rs = true
crates-io = true
license = true
codecov = true
