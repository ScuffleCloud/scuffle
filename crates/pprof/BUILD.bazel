load("//misc/utils/rust:manifest.bzl", "cargo_toml")
load("//misc/utils/rust:package.bzl", "scuffle_example", "scuffle_package")

cargo_toml()

scuffle_package(
    compile_data = [
        ":CHANGELOG.md",
        ":Cargo.toml",
    ],
    crate_name = "scuffle-pprof",
    proc_macro_deps = ["//crates/changelog"],
)

scuffle_example(
    name = "examples_cpu",
    srcs = ["examples/cpu.rs"],
)
