load("//misc/utils:binary_wrapper.bzl", "binary_wrapper")

binary_wrapper(
    name = "pnpm",
    extra_commands = [
        "set -- \"-C\" \"$(pwd)\" \"$@\"",
        "cd \"${pwd}\"",
    ],
    actual = "@multitool//tools/pnpm",
    visibility = ["//visibility:public"],
)
