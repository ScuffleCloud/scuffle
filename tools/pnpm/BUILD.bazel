load("//misc/utils:binary_wrapper.bzl", "binary_wrapper")

binary_wrapper(
    name = "pnpm",
    binary = "@pnpm//:pnpm",
    extra_commands = [
        "set -- \"-C\" \"$(pwd)\" \"$@\"",
        "cd \"${pwd}\"",
    ],
    visibility = ["//visibility:public"],
    tags = ["manual"],
)
